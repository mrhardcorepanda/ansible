---
- hosts: all
  tasks:
  - name: add grub stuff
    lineinfile:
      dest: /etc/default/grub
      line: "{{ item }}"
    loop:
      - "net.ifnames=0"
      - "biosdevname=0"
  - name: run grub2 thingy
    shell:
      cmd: grub2-mkconfig
      